---
import type { CollectionEntry } from 'astro:content';
import Layout from '../layout/Layout.astro';
import Container from '../ui/Container.astro';
import Card from '../ui/Card.astro';
import Button from '../ui/Button.astro';
import Hero from './Hero.astro';
import ValueProps from './ValueProps.astro';
import ReleaseProblems from './ReleaseProblems.astro';
import QualityLayer from './QualityLayer.astro';
import HowItWorks from './HowItWorks.astro';
import Stack from './Stack.astro';
import ServicesPreview from './ServicesPreview.astro';
import FAQ from './FAQ.astro';
import CTASection from './CTASection.astro';
import LogoCloud from './LogoCloud.astro';
import Testimonials from './Testimonials.astro';
import TierPricingTable from './TierPricingTable.astro';
import SnapshotTeaser from './SnapshotTeaser.astro';
import AutomationBridge from './AutomationBridge.astro';
import Check from 'lucide-astro/Check';
import Users from 'lucide-astro/Users';
import QuoteForm from '../forms/QuoteForm.astro';
import ContactForm from '../forms/ContactForm.astro';
import johnyPortrait from '../../assets/images/johny-fetramalala.jpg';
import { copy, getPageTitle, routeMap, type Lang } from '../../lib/i18n';
import { dedicated, localizedServiceIntro, plans, punctualOffers, services, site } from '../../lib/site';
import type { PunctualRow } from '../../lib/site';

interface Props {
	lang: Lang;
	path: string;
	page: 'home' | 'services' | 'service' | 'about' | 'cases' | 'blog' | 'quote' | 'contact' | 'legal' | 'privacy' | 'notFound';
	serviceId?: string;
	posts?: CollectionEntry<'blog'>[];
}

const { lang, path, page, serviceId, posts = [] } = Astro.props;
const t = copy[lang];
const service = services.find((item) => item.id === serviceId);
const schemaBase = { '@context': 'https://schema.org' };
const baseDescription = t.descriptions[page === 'notFound' ? 'home' : page === 'service' ? 'services' : page];
const title = page === 'service' && service ? service.title : page === 'notFound' ? (lang === 'fr' ? 'Page introuvable' : 'Page not found') : getPageTitle(lang, page === 'cases' ? 'cases' : page);
const ui = lang === 'fr'
	? {
		serviceButton: 'Voir le service',
		punctual: 'Ponctuel',
		monthly: 'Mensuel',
		dedicated: 'Dédié',
		deliverables: 'Ce que vous obtenez',
		process: 'Comment ça marche',
		forWhom: 'Pour qui',
		aboutKicker: 'Service QA premium',
		aboutTitle: 'À propos d’Arsenal Guard',
		aboutLead: 'Service QA premium pour produits SaaS, e-commerce et plateformes web européens.',
		missionTitle: 'Notre mission',
		missionTexts: [
			'Arsenal Guard est né d’une conviction : la qualité logicielle ne doit pas être un frein à la livraison. Elle doit devenir un filet clair, maintenable et utile aux équipes produit.',
			'Nous accompagnons des startups et scale-ups européennes sur l’automatisation des tests E2E, la stabilisation des suites existantes, l’intégration CI/CD et la priorisation des risques qualité, depuis nos bureaux à Antananarivo, Madagascar.',
		],
		whyTitle: 'Pourquoi Arsenal Guard ?',
		whyCards: [
			['Spécialisation QA pure', 'Nous ne sommes pas un cabinet généraliste. La QA est notre seul métier : Cypress, Playwright, CI/CD, stratégie de test. Une expertise pointue, pas de dilution.'],
			['Expertise francophone', 'Nos équipes parlent français nativement, comprennent vos enjeux métier et communiquent comme une extension de votre équipe interne.'],
			['Coût offshore optimisé', 'Bénéficiez d’une qualité d’expertise équivalente au marché européen, avec un avantage tarifaire significatif grâce à notre implantation à Madagascar.'],
			['Évolution sur le long terme', 'Nos tests sont conçus pour grandir avec votre produit. Votre équipe peut reprendre la main à tout moment, sans dépendance.'],
		],
		valuesTitle: 'Nos valeurs',
		values: [
			['Qualité', 'La qualité est notre obsession. Chaque test que nous écrivons doit apporter une vraie valeur business, jamais cocher une case sans impact.'],
			['Transparence', 'Nous facturons à la valeur, nous communiquons sans détour. Vous savez ce que vous payez, vous savez ce qui est livré.'],
			['Évolution', 'Vos tests d’aujourd’hui doivent rester utiles demain. Nous construisons des suites qui grandissent avec votre produit, pas qui finissent à la corbeille.'],
			['Fiabilité', 'Aucune régression critique ne doit atteindre votre production. C’est notre engagement et notre raison d’être.'],
		],
		madagascarTitle: 'Pourquoi Madagascar ?',
		madagascar: [
			['Équipe francophone native', 'Communication claire avec vos équipes produit, tech et management.'],
			['Recouvrement Europe', 'Le fuseau UTC+3 offre un overlap confortable avec la France, la Belgique, la Suisse et l’Europe.'],
			['Coût optimisé', 'Une expertise QA premium avec 40 à 50% d’économie par rapport à un staffing local équivalent.'],
			['Talents techniques formés', 'Un vivier d’ingénieurs issus notamment de formations informatiques reconnues comme l’ENI.'],
		],
		founderKicker: 'Notre fondateur',
		founderRole: 'Fondateur & Lead QA Engineer',
		founderTexts: [
			'Senior QA Engineer avec 5+ ans d’expérience sur des produits à fort enjeu. Diplômé en informatique, Master ENI Fianarantsoa.',
			'Johny a accompagné des plateformes utilisées par Chanel, Sodexo et plusieurs scale-ups SaaS européennes avant de fonder Arsenal Guard avec la mission de démocratiser la QA premium pour les équipes produit.',
		],
		aboutCtaTitle: 'Faisons connaissance autour de votre projet.',
		aboutCtaLead: 'Nous identifions vos risques release, vos parcours critiques et les premiers tests qui apporteront un vrai impact.',
		casesComing: 'Publication après validation client.',
		read: 'Lire',
		contactSlot: 'Lien de prise de rendez-vous à ajouter prochainement.',
		legalEditor: 'Éditeur : Arsenal Guard, service QA premium basé à Antananarivo, Madagascar.',
		legalData: 'Contact : {email}. Les données transmises via les formulaires sont utilisées uniquement pour répondre aux demandes commerciales. Vous pouvez demander leur suppression à tout moment.',
		legalHosting: 'Hébergement recommandé : Vercel ou Cloudflare Pages. Analytics prévu : Cloudflare Web Analytics ou Plausible, sans cookies publicitaires.',
		notFound: 'Cette page n’existe pas encore.',
		backHome: 'Retour accueil',
		qaNote: 'Automation · CI/CD · Risque',
	}
	: {
		serviceButton: 'View service',
		punctual: 'One-off',
		monthly: 'Monthly',
		dedicated: 'Dedicated',
		deliverables: 'What you get',
		process: 'How it works',
		forWhom: 'Best for',
		aboutKicker: 'Premium QA service',
		aboutTitle: 'About Arsenal Guard',
		aboutLead: 'Premium QA service for European SaaS, e-commerce and web platforms.',
		missionTitle: 'Our mission',
		missionTexts: [
			'Arsenal Guard was built on a simple belief: software quality should not slow delivery down. It should become a clear, maintainable safety net for product teams.',
			'From Antananarivo, Madagascar, we support European startups and scale-ups with E2E automation, flaky suite stabilization, CI/CD integration and risk-based QA prioritization.',
		],
		whyTitle: 'Why Arsenal Guard?',
		whyCards: [
			['Pure QA specialization', 'We are not a generalist consultancy. QA is our core craft: Cypress, Playwright, CI/CD and test strategy with deep focus.'],
			['Clear communication', 'Our teams communicate clearly with product, engineering and leadership so collaboration feels like an extension of your own team.'],
			['Optimized offshore cost', 'You get senior-level QA expertise with a significant pricing advantage thanks to our Madagascar-based model.'],
			['Built for the long run', 'Our tests are designed to evolve with your product, and your team can take over at any time without dependency.'],
		],
		valuesTitle: 'Our values',
		values: [
			['Quality', 'Every test we write must create real business value, not just tick a coverage box.'],
			['Transparency', 'We communicate directly and keep scope, priorities and deliverables visible.'],
			['Evolution', 'Today’s tests must remain useful tomorrow. We build suites that grow with your product.'],
			['Reliability', 'Critical regressions should not reach production. That is our commitment.'],
		],
		madagascarTitle: 'Why Madagascar?',
		madagascar: [
			['Strong communication', 'Clear collaboration with product, tech and management teams.'],
			['European overlap', 'UTC+3 provides comfortable overlap with France, Belgium, Switzerland and wider Europe.'],
			['Optimized cost', 'Premium QA expertise with 40 to 50% savings compared with equivalent local staffing.'],
			['Technical talent', 'A strong engineering talent pool from recognized computer science schools such as ENI.'],
		],
		founderKicker: 'Our founder',
		founderRole: 'Founder & Lead QA Engineer',
		founderTexts: [
			'Senior QA Engineer with 5+ years of experience on high-stakes products. Computer science graduate, Master ENI Fianarantsoa.',
			'Johny supported platforms used by Chanel, Sodexo and several European SaaS scale-ups before founding Arsenal Guard to make premium QA more accessible to product teams.',
		],
		aboutCtaTitle: 'Let’s map your project together.',
		aboutCtaLead: 'We identify release risks, critical journeys and the first tests that will create real impact.',
		casesComing: 'Publication after client approval.',
		read: 'Read',
		contactSlot: 'Booking link coming soon.',
		legalEditor: 'Publisher: Arsenal Guard, premium QA service based in Antananarivo, Madagascar.',
		legalData: 'Contact: {email}. Data submitted through forms is used only to answer business requests. You can request deletion at any time.',
		legalHosting: 'Recommended hosting: Vercel or Cloudflare Pages. Planned analytics: Cloudflare Web Analytics or Plausible, without advertising cookies.',
		notFound: 'This page does not exist yet.',
		backHome: 'Back home',
		qaNote: 'Automation · CI/CD · Risk',
	};
---

<Layout
	lang={lang}
	path={path}
	title={title}
	description={baseDescription}
	schema={[
		{ ...schemaBase, '@type': 'Organization', name: site.name, url: site.url, founder: site.founder },
		page === 'home' ? { ...schemaBase, '@type': 'ProfessionalService', name: site.name, areaServed: 'Europe', serviceType: 'QA automation and software testing' } : {},
		page === 'service' && service ? { ...schemaBase, '@type': 'Service', name: service.title, provider: { '@type': 'Organization', name: site.name }, offers: { '@type': 'Offer', price: service.price } } : {},
	]}
>
	{page === 'home' && (
		<>
			<Hero lang={lang} />
			<LogoCloud lang={lang} />
			<ReleaseProblems lang={lang} />
			<QualityLayer lang={lang} />
			<HowItWorks lang={lang} />
			<ValueProps lang={lang} />
			<SnapshotTeaser lang={lang} />
			<AutomationBridge lang={lang} />
			<Stack lang={lang} />
			<ServicesPreview lang={lang} mode="services" />
			<Testimonials lang={lang} />
			<FAQ lang={lang} />
			<CTASection lang={lang} />
		</>
	)}

	{page === 'services' && (
		<>
			<section class="bg-mesh py-24 md:py-32">
				<Container>
					<h1 class="max-w-4xl text-5xl font-extrabold text-white md:text-7xl">{t.nav.services}</h1>
					<p class="mt-6 max-w-3xl text-lg text-white/80">{localizedServiceIntro[lang]}</p>
				</Container>
			</section>
			<section class="py-24">
				<Container>
					<h2 class="mb-6 text-sm font-bold uppercase tracking-[0.14em] text-orange-400 reveal">
						{lang === 'fr' ? 'Tests manuels' : 'Manual testing'}
					</h2>
					<div class="grid gap-5 md:grid-cols-2">
						{services.filter((s) => s.id === 'test-manuel-ponctuel' || s.id === 'audit-qa').map((item) => (
							<Card>
								<p class="text-sm font-bold uppercase tracking-[0.14em] text-orange-400">{item.kicker}</p>
								<h2 class="mt-4 text-3xl font-extrabold">{item.title}</h2>
								<p class="mt-3 text-muted">{item.description}</p>
								<p class="mt-5 font-mono text-orange-400">{item.duration} · {item.price}</p>
								<Button class="mt-6" href={`services/${item.id}`} lang={lang} variant="secondary">{ui.serviceButton}</Button>
							</Card>
						))}
					</div>

					<h2 class="mb-6 mt-14 text-sm font-bold uppercase tracking-[0.14em] text-orange-400 reveal">
						{lang === 'fr' ? 'Automatisation & accompagnement' : 'Automation & retainers'}
					</h2>
					<div class="grid gap-5 md:grid-cols-2">
						{services.filter((s) => s.id !== 'test-manuel-ponctuel' && s.id !== 'audit-qa').map((item) => (
							<Card>
								<p class="text-sm font-bold uppercase tracking-[0.14em] text-orange-400">{item.kicker}</p>
								<h2 class="mt-4 text-3xl font-extrabold">{item.title}</h2>
								<p class="mt-3 text-muted">{item.description}</p>
								<p class="mt-5 font-mono text-orange-400">{item.duration} · {item.price}</p>
								<Button class="mt-6" href={`services/${item.id}`} lang={lang} variant="secondary">{ui.serviceButton}</Button>
							</Card>
						))}
					</div>
				</Container>
			</section>
			<section class="section-band border-y py-24">
				<Container class="grid gap-8 lg:grid-cols-3">
					<div>
						<h2 class="text-3xl font-extrabold">{ui.punctual}</h2>
						{punctualOffers.map((row: PunctualRow) =>
							'tiers' in row
								? (
									<div class="mt-4">
										<p class="font-semibold text-white">{row.name} · {row.duration}</p>
										{row.tiers.map(([label, price]) => (
											<p class="ml-3 mt-1 text-sm text-muted">{label} · <strong>{price}</strong></p>
										))}
									</div>
								)
								: <p class="mt-4 text-muted">{row[0]} · {row[1]} · <strong>{row[2]}</strong></p>
						)}
					</div>
					<div><h2 class="text-3xl font-extrabold">{ui.monthly}</h2>{plans.map((plan) => <p class="mt-4 text-muted">{plan.name} · {plan.volume} · <strong>{plan.price}</strong></p>)}</div>
					<div><h2 class="text-3xl font-extrabold">{ui.dedicated}</h2>{dedicated.map(([name, volume, price]) => <p class="mt-4 text-muted">{name} · {volume} · <strong>{price}</strong></p>)}</div>
				</Container>
			</section>
			<CTASection lang={lang} />
		</>
	)}

	{page === 'service' && service && (
		<>
			<section class="bg-mesh py-24 md:py-32">
				<Container>
					<p class="font-bold uppercase tracking-[0.16em] text-orange-400">{service.kicker}</p>
					<h1 class="mt-5 max-w-4xl text-5xl font-extrabold text-white md:text-7xl">{service.title}</h1>
					<p class="mt-6 max-w-3xl text-lg text-white/80">{service.description}</p>
					<p class="mt-8 font-mono text-xl text-orange-400">{service.duration} · {service.price}</p>
				</Container>
			</section>

			{service.tiers && service.tiers.length > 0 && (
				<TierPricingTable tiers={service.tiers} lang={lang} />
			)}

			<section class="svc-detail-section py-16 md:py-24">
				<Container>
					<!-- Bento row 1: deliverables (wide) + process (narrow) -->
					<div class="grid gap-4 lg:grid-cols-[1.65fr_1fr]">

						<!-- Deliverables -->
						<div class="svc-card reveal">
							<p class="svc-eyebrow">
								<Check class="size-3.5 flex-shrink-0" aria-hidden="true" />
								{ui.deliverables}
							</p>
							<ul class="svc-rows mt-5">
								{service.deliverables.map((item, i) => (
									<li class={`svc-row ${i > 0 ? 'svc-row--divide' : ''}`}>
										<span class="svc-row-icon">
											<Check class="size-3.5 text-orange-400" aria-hidden="true" />
										</span>
										<span class="text-sm leading-relaxed text-muted">{item}</span>
									</li>
								))}
							</ul>
						</div>

						<!-- Process steps -->
						<div class="svc-card reveal" style="--reveal-delay:80ms">
							<p class="svc-eyebrow">{ui.process}</p>
							<ol class="svc-rows mt-5">
								{service.process.map((item, i) => (
									<li class={`svc-row ${i > 0 ? 'svc-row--divide' : ''}`}>
										<span class="svc-step-num font-mono">0{i + 1}</span>
										<span class="text-sm leading-relaxed text-muted">{item}</span>
									</li>
								))}
							</ol>
						</div>
					</div>

					<!-- Bento row 2: For whom — full-width feature banner -->
					<div class="svc-for-whom mt-4 reveal" style="--reveal-delay:160ms">
						<div class="svc-for-whom-body">
							<p class="svc-eyebrow mb-4">
								<Users class="size-3.5 flex-shrink-0" aria-hidden="true" />
								{ui.forWhom}
							</p>
							<p class="text-base leading-relaxed md:text-lg">{service.for}</p>
						</div>
						<div class="svc-for-whom-cta">
							<Button href={routeMap.quote} lang={lang}>{t.nav.quote}</Button>
							<p class="mt-2.5 text-xs opacity-50">
								{lang === 'fr' ? 'Réponse sous 24h · Sans engagement' : 'Response within 24h · No commitment'}
							</p>
						</div>
					</div>
				</Container>
			</section>

			<FAQ lang={lang} items={service.faq} />

			{service.nextStep && (() => {
				const nextService = services.find((s) => s.id === service.nextStep!.serviceId)
				return nextService ? (
					<section class="section-band border-y py-16">
						<Container class="flex flex-col gap-4 md:flex-row md:items-center md:justify-between">
							<div>
								<p class="text-sm font-bold uppercase tracking-[0.14em] text-orange-400">
									{lang === 'fr' ? 'Étape suivante' : 'Next step'}
								</p>
								<p class="mt-2 text-lg font-semibold">{service.nextStep!.label[lang]}</p>
							</div>
							<Button href={`services/${nextService.id}`} lang={lang} variant="secondary" class="shrink-0">
								{nextService.title}
							</Button>
						</Container>
					</section>
				) : null
			})()}

			<CTASection lang={lang} />
		</>
	)}

	{page === 'about' && (
		<>
			<section class="about-hero py-24 md:py-32">
				<Container class="grid gap-10 lg:grid-cols-[1fr_.75fr] lg:items-center">
					<div>
						<p class="about-hero-kicker font-bold uppercase tracking-[0.16em]">{ui.aboutKicker}</p>
						<h1 class="about-hero-title mt-5 text-5xl font-extrabold md:text-7xl">{ui.aboutTitle}</h1>
						<p class="about-hero-copy mt-6 max-w-3xl text-xl">{ui.aboutLead}</p>
					</div>
					<div class="about-hero-card relative mx-auto grid aspect-square w-full max-w-md place-items-center rounded-2xl p-8 shadow-glow backdrop-blur">
						<div class="absolute inset-6 rounded-xl border border-orange-400/25"></div>
						<div class="about-hero-inner absolute inset-14 rounded-xl"></div>
						<div class="relative text-center">
							<p class="about-hero-mark text-6xl font-extrabold">QA</p>
							<p class="about-hero-note mt-3 text-sm font-bold uppercase tracking-[0.18em]">{ui.qaNote}</p>
						</div>
					</div>
				</Container>
			</section>
			<section class="py-24">
				<Container class="grid gap-8 lg:grid-cols-[.8fr_1.2fr]">
					<h2 class="text-3xl font-extrabold md:text-5xl">{ui.missionTitle}</h2>
					<div class="space-y-5 text-lg text-muted">
						{ui.missionTexts.map((text) => <p>{text}</p>)}
					</div>
				</Container>
			</section>
			<section class="section-band border-y py-24">
				<Container>
					<h2 class="text-3xl font-extrabold md:text-5xl">{ui.whyTitle}</h2>
					<div class="mt-10 grid gap-5 md:grid-cols-2">
						{ui.whyCards.map(([title, text]) => <Card><h3 class="text-xl font-bold">{title}</h3><p class="mt-3 text-muted">{text}</p></Card>)}
					</div>
				</Container>
			</section>
			<section class="py-24">
				<Container>
					<h2 class="text-3xl font-extrabold md:text-5xl">{ui.valuesTitle}</h2>
					<div class="mt-8 grid gap-5 md:grid-cols-4">
						{ui.values.map(([value, text]) => <Card><h3 class="text-xl font-bold">{value}</h3><p class="mt-3 text-muted">{text}</p></Card>)}
					</div>
				</Container>
			</section>
			<section class="section-band border-y py-24">
				<Container class="grid gap-8 lg:grid-cols-[.8fr_1.2fr]">
					<h2 class="text-3xl font-extrabold md:text-5xl">{ui.madagascarTitle}</h2>
					<div class="grid gap-4 md:grid-cols-2">
						{ui.madagascar.map(([title, text]) => <Card><h3 class="text-xl font-bold">{title}</h3><p class="mt-3 text-muted">{text}</p></Card>)}
					</div>
				</Container>
			</section>
			<Stack lang={lang} />
			<section class="py-24">
				<Container class="grid gap-10 lg:grid-cols-[.85fr_1.15fr] lg:items-center">
					<div class="about-portrait-group relative mx-auto w-full max-w-sm lg:mx-0">
						<div class="about-portrait-cage" aria-hidden="true"></div>
						<div class="about-portrait-glow absolute -inset-4 rounded-[2rem] bg-orange-400/20 blur-2xl" aria-hidden="true"></div>
						<div class="about-portrait-card relative overflow-hidden rounded-2xl border border-white/15 bg-white/10 p-3 shadow-glow backdrop-blur">
							<img src={johnyPortrait.src} alt={lang === 'fr' ? 'Portrait de Johny Fetramalala, fondateur d’Arsenal Guard' : 'Portrait of Johny Fetramalala, founder of Arsenal Guard'} width="1066" height="1026" decoding="async" class="about-portrait-img aspect-[4/5] w-full rounded-xl object-cover object-[50%_24%]" />
						</div>
					</div>
					<div>
						<p class="font-bold uppercase tracking-[0.16em] text-orange-400">{ui.founderKicker}</p>
						<h2 class="mt-4 text-3xl font-extrabold md:text-5xl">Johny Fetramalala</h2>
						<p class="mt-3 text-lg font-semibold">{ui.founderRole}</p>
						<div class="mt-6 space-y-4 text-lg text-muted">
							{ui.founderTexts.map((text) => <p>{text}</p>)}
						</div>
						<a class="mt-6 inline-flex rounded-xl border border-orange-400/40 px-5 py-3 font-bold text-orange-400 hover:bg-orange-400/10" href="https://linkedin.com/in/johny-qa">LinkedIn</a>
					</div>
				</Container>
			</section>
			<section class="bg-mesh py-20">
				<Container class="text-center">
					<h2 class="mx-auto max-w-3xl text-3xl font-extrabold text-white md:text-5xl">{ui.aboutCtaTitle}</h2>
					<p class="mx-auto mt-5 max-w-2xl text-white/80">{ui.aboutCtaLead}</p>
					<div class="mt-8"><Button href={routeMap.quote} lang={lang}>{t.nav.quote}</Button></div>
				</Container>
			</section>
		</>
	)}

	{page === 'cases' && <section class="py-24 md:py-32"><Container><h1 class="text-5xl font-extrabold md:text-7xl">{t.nav.cases}</h1><div class="mt-10 grid gap-5 md:grid-cols-3">{['Audit SaaS', 'Stabilisation E2E', 'Pipeline qualité'].map((item) => <Card><h2 class="text-2xl font-bold">{item}</h2><p class="mt-3 text-muted">{ui.casesComing}</p></Card>)}</div></Container></section>}

	{page === 'blog' && <section class="py-24 md:py-32"><Container><h1 class="text-5xl font-extrabold md:text-7xl">Blog</h1><div class="mt-10 grid gap-5 md:grid-cols-3">{posts.map((post) => <Card><p class="text-sm text-orange-400">{post.data.readTime}</p><h2 class="mt-4 text-2xl font-bold">{post.data.title}</h2><p class="mt-3 text-muted">{post.data.description}</p><Button class="mt-6" href={`blog/${post.slug.replace(/^\d+-/, '')}`} lang={lang} variant="secondary">{ui.read}</Button></Card>)}</div></Container></section>}

	{page === 'quote' && <section class="py-24 md:py-32"><Container class="grid gap-10 lg:grid-cols-[.8fr_1.2fr]"><div><h1 class="text-5xl font-extrabold md:text-7xl">{t.nav.quote}</h1><p class="mt-6 text-lg text-muted">{t.descriptions.quote}</p></div><QuoteForm /></Container></section>}

	{page === 'contact' && <section class="py-24 md:py-32"><Container class="grid gap-10 lg:grid-cols-[.8fr_1.2fr]"><div><h1 class="text-5xl font-extrabold md:text-7xl">{t.nav.contact}</h1><p class="mt-6 text-lg text-muted">{t.descriptions.contact}</p><p class="mt-6 font-mono text-orange-400">{site.email}</p><div class="surface mt-8 rounded-2xl p-6 text-muted">{ui.contactSlot}</div></div><ContactForm /></Container></section>}

	{(page === 'legal' || page === 'privacy') && <section class="py-24 md:py-32"><Container class="max-w-3xl"><h1 class="text-5xl font-extrabold">{page === 'legal' ? getPageTitle(lang, 'legal') : getPageTitle(lang, 'privacy')}</h1><div class="prose-ag mt-8"><p>{ui.legalEditor}</p><p>{ui.legalData.replace('{email}', site.email)}</p><p>{ui.legalHosting}</p></div></Container></section>}

	{page === 'notFound' && <section class="grid min-h-[70vh] place-items-center py-24"><Container class="text-center"><h1 class="text-6xl font-extrabold">404</h1><p class="mt-4 text-muted">{ui.notFound}</p><Button class="mt-8" href="/" lang={lang}>{ui.backHome}</Button></Container></section>}
</Layout>

<style>
/* ── Service detail bento layout ──────────────────────────── */
.svc-card {
	border: 1px solid rgba(255, 255, 255, 0.08);
	border-radius: 1.5rem;
	background: rgba(255, 255, 255, 0.04);
	backdrop-filter: blur(20px) saturate(150%);
	-webkit-backdrop-filter: blur(20px) saturate(150%);
	padding: 2rem;
	box-shadow:
		0 1px 0 rgba(255, 255, 255, 0.07) inset,
		0 24px 64px rgba(5, 11, 26, 0.18);
	transition:
		border-color 200ms ease,
		box-shadow 200ms ease;
}

:global([data-theme='light']) .svc-card {
	background: rgba(255, 255, 255, 0.92);
	border-color: rgba(15, 31, 68, 0.1);
	box-shadow:
		0 1px 0 rgba(255, 255, 255, 0.8) inset,
		0 4px 28px rgba(15, 31, 68, 0.07);
}

.svc-eyebrow {
	display: inline-flex;
	align-items: center;
	gap: 0.45rem;
	font-size: 0.68rem;
	font-weight: 800;
	letter-spacing: 0.18em;
	text-transform: uppercase;
	color: #f39c12;
}

:global([data-theme='light']) .svc-eyebrow {
	color: #c86f12;
}

.svc-rows {
	list-style: none;
	padding: 0;
	margin: 0;
}

.svc-row {
	display: flex;
	align-items: flex-start;
	gap: 0.85rem;
	padding: 0.85rem 0;
}

.svc-row--divide {
	border-top: 1px solid rgba(255, 255, 255, 0.06);
}

:global([data-theme='light']) .svc-row--divide {
	border-top-color: rgba(15, 31, 68, 0.07);
}

.svc-row-icon {
	flex-shrink: 0;
	margin-top: 0.15rem;
}

.svc-step-num {
	flex-shrink: 0;
	font-size: 0.75rem;
	font-weight: 700;
	letter-spacing: 0.06em;
	color: #f39c12;
	opacity: 0.65;
	margin-top: 0.15rem;
	min-width: 1.75rem;
}

:global([data-theme='light']) .svc-step-num {
	color: #c86f12;
}

/* ── For whom — gradient feature banner ───────────────────── */
.svc-for-whom {
	border: 1px solid rgba(230, 126, 34, 0.2);
	border-radius: 1.5rem;
	background: linear-gradient(
		130deg,
		rgba(230, 126, 34, 0.1) 0%,
		rgba(15, 31, 68, 0.7) 45%,
		rgba(10, 21, 48, 0.88) 100%
	);
	backdrop-filter: blur(20px) saturate(150%);
	-webkit-backdrop-filter: blur(20px) saturate(150%);
	padding: 2rem 2.25rem;
	display: flex;
	flex-direction: column;
	gap: 1.75rem;
	box-shadow:
		0 1px 0 rgba(255, 200, 100, 0.08) inset,
		0 0 0 1px rgba(230, 126, 34, 0.06),
		0 24px 64px rgba(5, 11, 26, 0.22);
}

@media (min-width: 768px) {
	.svc-for-whom {
		flex-direction: row;
		align-items: center;
		justify-content: space-between;
		padding: 2.25rem 2.75rem;
	}
}

.svc-for-whom-body {
	max-width: 44rem;
	color: rgba(255, 255, 255, 0.9);
}

.svc-for-whom-cta {
	flex-shrink: 0;
	display: flex;
	flex-direction: column;
	align-items: flex-start;
}

@media (min-width: 768px) {
	.svc-for-whom-cta {
		align-items: center;
	}
}

:global([data-theme='light']) .svc-for-whom {
	background: linear-gradient(130deg, rgba(230, 126, 34, 0.07) 0%, #ffffff 55%);
	border-color: rgba(211, 84, 0, 0.22);
	box-shadow:
		0 0 0 1px rgba(211, 84, 0, 0.06),
		0 20px 56px rgba(15, 31, 68, 0.09);
}

:global([data-theme='light']) .svc-for-whom-body {
	color: #182033;
}
</style>
