---
interface Props {
	class?: string;
}
const { class: className = '' } = Astro.props;
---

<span class={`inline-flex items-center rounded-full border border-orange-400/30 bg-orange-400/10 px-3 py-1 text-xs font-bold uppercase tracking-[0.16em] text-orange-400 ${className}`}>
	<slot />
</span>
